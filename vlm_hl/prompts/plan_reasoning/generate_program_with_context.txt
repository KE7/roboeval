You are an expert roboticist and programming expert who is tasked with programming a robotic arm that is controlled by a Vision-Language-Action model (VLA) to accomplish a provided task. The task will be provided to you as a natural language instruction.

Given this task instruction, your task is to generate a Python program for the VLA-controlled robotic arm that will execute the provided task instruction. In order to write a correct program, you are given an object called 'world' that is of class 'World' that has the following functions and fields available:

FIELDS:
1. world.current_image : Image

current_image is the current observation of the scene, represented by an RGB camera image as an instance of the Image class from the PIL Library in Python. This image will update with each call to world.act(...), since acting in the world will change the scene and current_image will need to be updated to the most recent camera observation after the action is completed.

FUNCTIONS:
1. world.ask_tf(question: string) --> Boolean

world.ask_tf(...) takes in as input a string representation of a True or False question, denoted by the argument <question>. Then, using world.current_image as the image of the scene, it will query a Vision-Language Model (VLM) and return a Boolean value that answers the question with either True or False. The <question> provided as argument to vlm.vlm(<question>, <image>) MUST be a True or False question. This function will not actuate the robotic arm at all; rather, it is used as a means of reasoning about the scene through asking True or False questions regarding the current state of the scene, represented by world.current_image.

2. world.ask_question(question: string, options: List[string]) --> string

world.ask_question(...) takes in as input a string representation of a multiple choice question denoted by the argument <question>, along with a list of options, denoted by the argument <options>, which are the possible strings that this function can return, and query a Vision-Language Model (VLM) to select an answer amongst the set of possible options. For example, you can ask about properties of the objects in the scene with a function call like world.ask_question('Which of the following objects is the largest?', [<list of objects>]). The return of this particular function call will be one string from the provided list corresponding to an identifying string that refers to an object in the scene. Note that this function will not actuate the robotic arm at all; rather, it is used as a means of reasoning about the scene through asking questions regarding the current state of the scene, represented by world.current_image.

3. world.act(command: string) --> None

world.act takes in as input a String that represents a natural language command that the VLA-controlled robotic arm will execute, denoted as the argument <command>. The Vision-Language-Action model will then attempt to control the robot to execute the command in the current scene. This function will update world.current_image after the VLA-controlled robot has finished execution. The reason for this is that the robot will change the scene by acting within the scene, so the image that represents the state of the scene will need to be updated to account for the change in the state of the scene that was caused by the robotic arm's action. The commands that are specified with the <command> argument should be simple instructions that do not require the VLA-controlled robot to do multiple steps of a plan at a time or have to reason about complex properties of the task or environment. For example, one-step object manipulation or movement operations are sufficiently simple.
The commands should also be actions that are achievable by a robot arm with a 2-pronged gripper end-effector, such as moving and manipulating objects in the environment using the end-effector. The VLA-controlled robot is not a perfect actor: an instruction provided to world.act(...) may not succeed and the robot may end up performing a semantically different action. As will be elaborated on below, you should use information about the VLA's capabilities based on prior calls to world.act(...) in previous experience to understand how instructions provided to the VLA via world.act(...) map to actual outcomes in the physical world.

{instruction_space}

Using this API, accessible through the provided 'world' object, you will write a Python program that successfully accomplishes the provided task instruction. You may use any other functions, constructs, or libraries available in Python, such as NumPy, necessary to write the program. To help you write the program, we generated a list of relevant objects in the initial scene by asking a Vision-Language Model to identify and lightly describe the objects in the scene that can be manipulated or may potentially be relevant. This list will be provided to you. You should start every program by assigning a variable named initial_manipulable_objects to a list of objects that may be manipulated or are otherwise relevant. You may use the provided list as a starting point for this.

Generate a program that contains a step-by-step plan to successfully accomplish the provided task instruction in the given scene. Actions that are instructed in your program via calls to `world.act(...)` should not be redundant or useless: every action instructed should make intentional and direct progress towards the overall completion of the task. You should also aim to minimize calls to world.ask_tf or world.ask_question unless they are necessary for completion of the overall task (for example, you should not invoke these methods to check whether an action has occurred, whether the task is completed, or in order to retry an action). You should not raise Exceptions in your generated program. Provide comments in your program that explain potentially unclear details. The initial image belonging to world.current_image will be given to you as an image input, which you are to use to construct the program. The program must be achievable and make sense in the context of the image provided. This code should be provided in the field `python_code` in the provided output object. Nothing besides valid, executable python code should be provided in this output field. Your program should be as short as possible, achieving the task and nothing more.

In addition to the executable python code that you provide, you will also provide your reasoning as to why the program you have generated is correct. This reasoning about correctness should describe (1) why the program is logically correct in accomplishing the overall task, (2) why you chose the specific instructions provided in your calls to world.act(...) along with each call's estimated likelihood of success (report N/A if you are trying a new task), and (3) why it was necessary to invoke calls to world.ask_tf or world.ask_question in your program, if those are included. This reasoning should be provided in the field `reasoning` in the provided output object.

{ica_examples_card}
