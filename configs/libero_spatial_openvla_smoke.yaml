# roboeval smoke config: OpenVLA x libero_spatial (10 episodes per task, 1 task)
#
# Usage:
#   roboeval serve --vla openvla --sim libero --headless &
#   roboeval run -c configs/libero_spatial_openvla_smoke.yaml

name: libero_spatial_openvla_smoke

# VLA server (openvla default port is 5101; avoids collision with pi05 on 5100)
vla: openvla
vla_url: http://localhost:5101

# Sim
sim: libero
sim_url: http://localhost:5300
suite: libero_spatial

# Episodes
episodes_per_task: 10
max_tasks: 1

# VLM
no_vlm: true

# Action
delta_actions: true

# Python for subprocess (defaults to sys.executable if empty)
eval_python: ""

# Output
output_dir: results/libero_spatial_openvla_smoke

# Extra params passed to roboeval.run_sim_eval (key -> value)
params: {}
